<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <!-- Deliberately UI-free so the timer and report logic can be tested
         headlessly and shared by the WinUI app and the report-dump tool. -->
  </PropertyGroup>
</Project>
